jQuery(document).ready(function() {
/*
var colours = [
"#bf0404", // rouge sombre  
"#0411bf", //bleu sombre
"#1abf04", //vert misombre
"#195d10", //vert sombre
"#891122", //marron rose
"#bf3145", //rose pale
"#338cb1", //bleu turquoise sombre
"#33b177", //vert d'eau
"#e68217", //orange
"#e34f22", //orange saumon
"#9322e3", //violet de marylène
"#d822e3", //violet rose
"#68d526", //vert fluo
"#d0d71d", //jaune gris
"#ffb400", //orange jaune clair
"#0cff00", //autre vert fluo eau
"#00ffde", //bleu turquoise
]; 
var idx = 0;
	var idAnte = -1;
    
    jQuery('h4').each(function(){
    //if statement here 
    // use jQuery(this) to reference the current div in the loop
    //you can try something like...
    var div = jQuery(this);
    var chars = jQuery(this).text().split('');
       div.html('');     
    for(var i=0; i<chars.length; i++) {
		idx = Math.floor(Math.random() * colours.length);
        while (idx == idAnte){
		idx = Math.floor(Math.random() * colours.length);
		}
        var span = jQuery('<span>' + chars[i] + '</span>').css("color", colours[idx]);
        div.append(span);
		idAnte=idx ; 
    }
 });

    jQuery(".belowlogo-text").each(function(){
    //if statement here 
    // use jQuery(this) to reference the current div in the loop
    //you can try something like...
    var div = jQuery(this);
    var chars = jQuery(this).text().split('');
       div.html('');     
    for(var i=0; i<chars.length; i++) {
		idx = Math.floor(Math.random() * colours.length);
        while (idx == idAnte){
		idx = Math.floor(Math.random() * colours.length);
		}
        var span = jQuery('<span>' + chars[i] + '</span>').css("color", colours[idx]);
        div.append(span);
		idAnte=idx ; 
    }
 });

*/
    
});